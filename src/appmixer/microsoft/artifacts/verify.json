{
    "fixtures": {
        "messageId": { "from": "ListEmails", "path": "id" },
        "attachmentId": {
            "from": "GetAttachments",
            "inputs": { "messageId": "{messageId}" },
            "path": "id"
        }
    },
    "read": [
        { "component": "ListEmails" },
        { "component": "ListFolders" },
        { "component": "ListCategories" },
        { "component": "GetEmail", "inputs": { "messageId": "{messageId}" } },
        { "component": "GetAttachments", "inputs": { "messageId": "{messageId}" } },
        {
            "component": "DownloadAttachment",
            "inputs": { "messageId": "{messageId}", "attachmentId": "{attachmentId}" }
        }
    ]
}
