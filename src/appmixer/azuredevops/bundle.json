{
    "name": "appmixer.azuredevops",
    "version": "1.3.2",
    "changelog": {
        "1.0.0": [
            "Initial release."
        ],
        "1.1.0": [
            "auth: add optional Tenant ID field to support multi-tenant Azure AD setups (#2684)",
            "components: return a clear error when the API response is not a valid object (prevents engine TypeError)"
        ],
        "1.1.1": [
            "Added output examples to component schemas."
        ],
        "1.2.0": [
            "Added test() (Flow Test Mode) to the NewWorkItem and UpdatedWorkItem triggers."
        ],
        "1.3.0": [
            "Added MakeApiCall component for performing arbitrary authorized API calls to the Azure DevOps API."
        ],
        "1.3.1": [
            "Added missing output examples to CreateWorkItem, GetWorkItem and UpdateWorkItem."
        ],
        "1.3.2": [
            "NewWorkItem and UpdatedWorkItem: work item fields are now also available nested (resource.fields.System.Title, resource.revision.fields.System.State, …) next to the dotted keys Azure DevOps sends, so the declared outputs resolve; examples moved into the output schemas and corrected (event id, updated event type)."
        ]
    }
}
