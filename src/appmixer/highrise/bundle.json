{
    "name": "appmixer.highrise",
    "version": "1.3.2",
    "changelog": {
        "1.0.1": [
            "Initial version"
        ],
        "1.1.0": [
            "Added test() (Flow Test Mode) to all triggers: NewContact, NewDeal, NewTask, NewCase."
        ],
        "1.2.0": [
            "Added test() (Flow Test Mode) to all triggers: SyncSource."
        ],
        "1.3.0": [
            "Added MakeApiCall component for performing arbitrary authorized API calls to the Highrise API."
        ],
        "1.3.1": [
            "NewCase, NewDeal and NewTask get output schemas with examples matching what the triggers emit; their company picker is a typeahead."
        ],
        "1.3.2": [
            "CreateCompany, CreateContact, CreateContactNote, CreateTask, NewContact and SyncSource get output examples; SyncSource's fields point at `data.*`, where the contact is emitted (they resolved to nothing before), and `phase` / `source` are declared. The Create components reject missing required inputs up front; the company picker is a typeahead."
        ]
    }
}
