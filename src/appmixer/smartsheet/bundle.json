{
    "name": "appmixer.smartsheet",
    "version": "2.3.4",
    "changelog": {
        "1.0.1": [
            "Smartsheet"
        ],
        "2.0.1": [
            "BugFix: Update of path to ListHomeFolders in multiple component's inspectors.",
            "Breaking: Added input fields of isAdmin and licensedSheetCreator in AddUser as they are required fields from the API.",
            "Cleanup of descriptions of all components.",
            "Breaking: Updated input field of GetFolders to have multiselect instead of single select.",
            "BugFix: Fixed GetSheet."
        ],
        "2.0.5": [
            "Breaking change: Updated the input ports of AddRowsToSheet to fix the display of correct columns to input value in the inspector",
            "Breaking change: Bug fix in input fields of Copyfolder and MoveFolder",
            "Breaking change: Updated the input ports of UpdateRow to fix the display of correct columns to input value in the inspector",
            "Bug fix: Fixed the MoveRow component"
        ],
        "2.1.0": [
            "Implemented multiselect input normalization for include and skipRemap fields in CopyFolder, CopySheet, GetFolder, GetWorkspace, and MoveRows components."
        ],
        "2.1.1": [
            "Added output examples to component schemas."
        ],
        "2.2.0": [
            "Added test() (Flow Test Mode) to the WatchRows trigger."
        ],
        "2.3.0": [
            "Added MakeApiCall component for performing arbitrary authorized API calls to the Smartsheet API."
        ],
        "2.3.1": [
            "Added missing output examples to ListContacts, ListHomeContents, ListHomeFolders, ListSheets, ListUsers, ListWebhooks, ListWorkspaces and WatchRows."
        ],
        "2.3.2": [
            "AddRowToSheet and UpdateRows: the sheet picker is now a typeahead text field, so a value can still be entered when the list fails to load; added output examples."
        ],
        "2.3.3": [
            "AddUser, CopySheet, GetContact, GetFolder, GetSheet, GetWorkspace, GetWorkspaceFolders, ListSearch, ListSearchSheet, MoveSheet, UpdateFolder and UpdateWorkspace now stop with a clear error when a required input is missing instead of calling the API; added output schemas with examples."
        ],
        "2.3.4": [
            "Fixed outputs that never resolved: GetCurrentUser / GetUser declare alternateEmails as the list the API returns (the alternateEmails.* paths pointed into it), GetCurrentUser no longer declares groups it never requests, CopyFolder / MoveFolder declare the response (message, resultCode, result) instead of request fields. Required-input checks for GetRow, GetUser, CopyFolder, MoveFolder, ListColumnsOnSheet and ListFolders; typeahead folder picker; output schemas with examples."
        ]
    }
}
